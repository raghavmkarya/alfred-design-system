# macOS
.DS_Store
**/.DS_Store

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Node / build artifacts (none committed today; guard against future tooling)
node_modules/
.playwright-mcp/

# Social frame exports (regenerate with scripts/capture-social.mjs)
social/_exports/

# Session scratch, deliberately untracked and not synced. Listing them here
# because `git add -A` has staged them by accident before.
HANDOFF.md
mocks/

# Playwright
test-results/
playwright-report/
blob-report/
dist
